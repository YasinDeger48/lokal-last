# lokal-last

This project is managed by TestFlow Studio.

- Put your Playwright specs inside `tests/`
- Use the Recorder page to generate specs into `tests/recordings/`
- Run tests from the Test Runner inside TestFlow Studio
